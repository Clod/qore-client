flutter packages pub run build_runner watch

watch parameter tell the tool to keep running and monitoring
for changes in router class.

For one time use:
flutter packages pub run build_runner build

flutter build web

npm install -g http-server
cd web
http-server

este es el new
